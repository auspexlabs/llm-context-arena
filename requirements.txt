# Curia backend Python dependencies — pip convenience mirror.
#
# Source of truth is pyproject.toml + uv.lock; `uv sync` installs a fully
# locked environment and is the recommended path. This file mirrors the
# runtime dependencies declared in [project.dependencies] for pip-based setups.
# Keep it in sync with pyproject.toml when dependencies change.
#
# torch is platform-specific — install it FIRST for your platform (CUDA / CPU /
# Apple Silicon) as shown in the README, then run `pip install -r requirements.txt`
# (the pinned torch below is then already satisfied). Installing this file
# without a prior torch resolves torch==2.11.0 from the default index, which may
# not carry a wheel for every platform/CUDA combination.

# Web / API
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
python-multipart>=0.0.32
httpx>=0.27.0
pydantic>=2.9.0
python-dotenv>=1.0.0
pyyaml>=6.0

# Retrieval / RAG
langchain-community>=0.3.0
langchain-core>=0.3.0
langchain-openai>=0.2.0
langchain-text-splitters>=0.3.0
faiss-cpu>=1.8.0
networkx>=3.0
sentence-transformers>=3.0.0
einops>=0.8.0
pylate>=1.6.0
voyager>=2.1.0
torch==2.11.0  # install per-platform first (see README): CUDA / CPU / Apple Silicon

# AST chunking (tree-sitter grammars)
tree-sitter>=0.23.0
tree-sitter-python>=0.23.0
tree-sitter-rust>=0.23.0
tree-sitter-javascript>=0.23.0
tree-sitter-typescript>=0.23.0
tree-sitter-go>=0.23.0

# Agent control plane
mcp>=1.28.1
